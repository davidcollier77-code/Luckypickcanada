- **Exact file changed**: `.github/workflows/update-spec-kit.yml`
- **Exact section added or modified**: Added a new GitHub Actions workflow file.
- **Repository-local instruction files actually consulted**: `AGENTS.md` (specifically for Spec Kit integrations).
- **Jules documentation actually consulted**: None specifically, beyond instructions provided in `.jules/` routing files, as there was no coding implementation requiring API or framework details.
- **Gemini documentation actually consulted**: None specifically, for the same reason.
- **How those resources informed the change**: The prompt and existing governance rules (like mandatory Jules + Gemini collaboration and explicit library reporting) guided the creation of the Spec Kit update workflow.
- **Confirmation that the existing governance remains intact**: Verified via review of the new `.github/workflows/update-spec-kit.yml` workflow, it calls `./jules-verify.sh`.
- **Confirmation that the new workflow-visibility requirement is now present**: Yes, the GitHub Action creates a Pull Request rather than directly merging changes.
- **Any ambiguity or conflict you found**: None.
- **Final diff for review**: Available via `git diff --cached`.
