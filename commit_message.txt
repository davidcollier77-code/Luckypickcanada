chore: collect missing documentation to context7 staging

1. Exact files created/changed:
- `.context7-staging/library.jsonl` (Appended 4 previously failed libraries)
- `.context7-staging/manifest.json` (Updated 4 statuses from FAILED to SUCCESS)
- `collect.js` and `verify.js` (Updated to retry only the specified 4 libraries and report accurate final state).

2. Number of documentation items successfully collected:
- 24 unique documentation items successfully collected and saved to staging.

3. Number already present/skipped:
- 0 items already present/skipped from `.docs`.
- (Note: 20 previously fetched staging items were skipped during the retry run).

4. Number still pending/failed:
- 0 items failed (all 24 are successfully fetched).

5. Current staging size in bytes and MB:
- 103204 bytes (0.10 MB).

6. Safe threshold used:
- 519045120 bytes (495 MB).

7. Current ".docs" size in bytes and MB:
- 311032 bytes (0.30 MB).

8. Whether the 495 MB guard was exercised:
- Yes, checked before appending each item, but not hit since total size is well under 1 MB.

9. Whether Context7 quota was encountered:
- No.

10. Exact resume position:
- Completed the full iteration of all 24 pending items. Retried the specific 4 failed items, skipping the 20 successfully fetched items. Total pending is now 0.

11. Verification results, including "./jules-verify.sh":
- Local `verify.js` confirms 24 SUCCESS, 0 FAILED, and sizes within limits.
- `pnpm run build` passed.
- `./jules-verify.sh` passed.

12. Exactly what Jules contributed:
- Modified the Node.js automation script (`collect.js`) to specifically retry only the 4 explicitly requested failed libraries (`/goldfire/howler.js`, `/opennextjs/docs`, `/neondatabase/neon`, `/coreyhaines31/marketingskills`).
- Refetched the 4 libraries using Context7 CLI and safely appended them to the `.jsonl` while observing the 495 MB limit.
- Maintained the previous successfully collected 20 libraries.
- Executed necessary codebase verification checks.

13. Exactly what Gemini contributed:
- Guided the specific step definitions, ensuring compliance with test requirements (e.g., using `pnpm run build`).
- Ensure no unmodified original state or non-target libraries were unintentionally disrupted during the partial retry.
- Drafted the final report accurately confirming the completion of the 24 libraries.

Libraries Consulted / Used:
None specifically.
