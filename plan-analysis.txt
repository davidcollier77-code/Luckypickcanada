A — VERIFIED ANALYSIS

1.  **Verified Facts:**
    *   **Button Latency:** `soundsRef.current.uiClick.play()` triggers immediately on button press in `handleReveal`, but the cinematic `buildup` sound is delayed by exactly `150ms`. The visual percentage buildup starts at 1.5s into the timeline. The perceived "pause after the click" is governed by this `150ms` delay on the `buildup` audio.
    *   **"impactMeteor" Reuse:** The `impactMeteor` sound currently points to `/sounds/mixkit-cinematic-whoosh.mp3`. It is only used in the `Meteor Shower` tier logic (`spawnMeteor` loops). Previous PRs mapped fireworks to `fireworkLaunch`, so `impactMeteor` is no longer reused for fireworks in this file.
    *   **Meteor Timing:** Current meteor spawn timings are `200ms`, `800ms`, `1600ms`, `2200ms`, `3200ms`. The first meteor at `200ms` is very close to the reveal transition (which happens at `0ms` relative to the canvas loop start). This spacing is tight.
    *   **Meteor Audio Asset:** The current asset (`mixkit-cinematic-whoosh.mp3`) is a "generic whoosh". To replace it with a heavy atmospheric entry sound, I will need to procure one from a CC0 source (like Pixabay/Freesound) using `wget` into `public/sounds/`, as no existing unused asset fits the description perfectly.

2.  **Assumptions vs. Facts:**
    *   *Fact:* While 150ms is a delay, the visual "generator/rev" (aurora phase 'awaken') starts immediately at `0ms` on the timeline. If the click feels separated from the rev, reducing this delay to ~50-75ms will tighten the feeling without overlapping transients.
    *   *Fact:* `tierAudioKey` is set during GSAP initialization but never actually used to play sound in the GSAP timeline. All impact sounds are triggered directly inside the `requestAnimationFrame` loop (`animateCanvas`).

B — BOUNDARIES AND PLAN

1.  **Approved Scope:**
    *   **Latency:** Reduce the `buildup` audio delay from `150ms` to `75ms` to tighten the separation between the physical click and the generator rev, making it "very, very brief".
    *   **Meteor Sourcing:** Procure and replace the meteor sound with a heavy atmospheric entry sound (Pixabay/Freesound CC0). I will use `wget` to pull a specific CC0 "meteor strike / atmospheric tearing" sound into `public/sounds/` and update `DailyResonance.tsx` to use it.
    *   **Meteor Timing:** Adjust meteor spawn timings in `DailyResonance.tsx` to: `400ms`, `1400ms`, `2600ms`, `3600ms`, `4800ms`. This pushes the first meteor back slightly to follow the reveal transition naturally and adds deliberate breathing room between subsequent meteors to prevent audio mud.
    *   **Cleanup:** Remove unused `tierAudioKey` logic from the GSAP setup.
    *   **Audio Overlap:** Use Howler's instance manipulation (already present) combined with the new asset to ensure controlled decay.

2.  **Protected Areas:**
    *   Do not touch Lightning or Fireworks timing/assets.
    *   Do not change the 3-tier percentage thresholds (`<=35`, `<=74`, `>74`).
    *   Preserve existing GSAP visual timing.

🔴 STOP HERE AND WAIT FOR MY EXPLICIT APPROVAL BEFORE EXECUTING THE PLAN.
