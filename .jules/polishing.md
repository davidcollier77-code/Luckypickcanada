# Polishing Guidelines
For UI/UX, visual refinement, animation, interaction, responsive behavior, and accessibility.

- Strict adherence to Tailwind utility classes. Do not write custom CSS unless absolutely necessary (e.g., complex keyframes for Auroras).
- For animations, leverage `framer-motion` for declarative, staggered effects.
- For audio feedback, use `Howler.js`.
