# Testing Guidelines
For testing, QA, regression checking, validation, and review.

- Use `vitest` and `@testing-library/react` for unit and component testing.
- Playwright is reserved for E2E flows and visual testing.
- Always run `pnpm run build` and `./jules-verify.sh` to validate the environment.
