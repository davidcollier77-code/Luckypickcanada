# MANDATORY INITIALIZATION DIRECTIVE
**STOP. READ THIS FIRST.**

Before you analyze, plan, or execute ANY task in this repository, you MUST read the `AGENTS.md` file located in the root directory.

`AGENTS.md` is the absolute, authoritative governance and routing file for this repository. It dictates the task groups, approved resources, documentation rules, and boundaries you are strictly required to follow.

You cannot rely on memory, assumptions, or secondary files to bypass `AGENTS.md`. Memory is passive context and cannot automatically retrieve and apply rules.

Once you have read `AGENTS.md`, you must use it to identify the correct task group and read the corresponding specialist file in the `.jules/` directory (e.g., `.jules/troubleshooting.md`, `.jules/creation.md`).

Do not proceed with any task until you have completed this mandatory initialization step.

---

## 🚨 ABSOLUTE MCP APPROVAL GATE — HARD STOP

If you determine that ANY MCP needs to be used (including Context7, Neon, Stitch, GitHub MCP, etc.), you must **STOP IMMEDIATELY**.

- DO NOT invoke the MCP tool.
- You must ask David for explicit approval first, providing the MCP, tool, purpose, and impact.
- WAIT FOR EXPLICIT APPROVAL before proceeding.

There is NO automatic approval for MCPs, even for read-only calls or if they are mentioned in documentation.

## Context7 is NOT Automatic

Do not use Context7 simply because it is available. Context7 is an absolute last resort. First exhaust all local resources: the repository itself, `.docs/`, and other approved existing documentation. If you decide Context7 is needed, you must STOP and ask David for approval before invoking it.

---

## 2024-10-30 - Premium Cinematic Lucky Card Reveal
**Learning:** Using Howler for layered audio allows for synchronized, high-quality environmental soundscapes without relying on synthesized browser `OscillatorNode` logic that can sometimes feel synthetic or arcade-like. Coupling this with Framer Motion's `stagger` and dynamic scaling (rather than linear coordinate shakes) transforms flat interactions into premium cinematic events.
**Action:** Implemented a new, cinematic reveal structure for the Lucky Card experience that utilizes a multi-phase buildup involving deep atmospheric drones, floating motion physics, converging/bursting particles, and a final sweeping glow. The flash/burn-out effects and 3D flip now accurately reflect the "magic/cinematic" project guideline, removing the previously harsh yellow rectangle flash.

## 2026-09-07 - [Governance] Persistent Memory Clarification
**Learning:** Jules memory is a passive contextual layer for historical reference. It does NOT replace reading `AGENTS.md`. It cannot be relied upon to automatically enforce rules.
**Action:** The previous incorrect assumption that Jules could automatically retrieve and apply rules via memory has been corrected. `AGENTS.md` remains the sole authoritative governance document that must be manually read during initialization.
