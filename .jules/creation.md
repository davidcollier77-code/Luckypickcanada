# Creation Guidelines
For creating new pages, features, components, systems, assets, or substantial functionality.

- Prioritize Next.js App Router standards (Server Components by default).
- Only use `use client` when interactivity or hooks are strictly required.
- Do not introduce new dependencies unless they provide unique, non-redundant capabilities and fit the existing stack.
