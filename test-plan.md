1. **Analyze**: The card's initial state for the reveal animation uses `{ duration: 0.1 }` to animate opacity to 0 and brightness to 0. This causes the card to fade out over 0.1s (or remain visible if RAF is delayed) immediately after the "Reveal" button is clicked, making the fully-formed card visible during the initial loading/forging phase.
2. **Fix**: Update `sequence.push` in `triggerCardDraw` to use `opacity: [0, 0]` and `filter: ["brightness(0)", "brightness(0)"]` with `{ duration: 0.001 }` to force immediate hiding. Furthermore, directly apply `opacity = "0"` and `filter = "brightness(0)"` to `cardRef.current.style` inside `triggerCardDraw` for synchronous, instant DOM update before the next paint.
