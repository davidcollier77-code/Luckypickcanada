JULES + GEMINI - SPEC KIT WORKFLOW REPAIR REPORT

**What Jules did:**
1. Inspected `.specify/integration.json` to understand the generic integration settings, which set the commands directory to `.jules/cmds`.
2. Inspected the command markdown files in `.jules/cmds/` (e.g., `speckit.specify.md`, `speckit.plan.md`, `speckit.tasks.md`) to understand their invocation model and the expected AI behavior.
3. Determined that Jules lacks a native CLI command to execute these `.md` files as programs. The execution mechanism requires the AI agent to explicitly read the file (e.g., using `cat`) and follow the natural language instructions contained within.
4. Appended Section "19. Spec Kit Workflow Integration" to `AGENTS.md` without modifying any other rules. This new section instructs Jules on exactly how to execute Spec Kit workflows by reading the command files and following instructions.
5. Clarified the workflow order in the new section: Spec Kit (Specification/Planning) -> Task Group Routing -> Resource Selection -> Implementation/Testing/Verification.
6. Conducted a non-destructive test using `SPECIFY_FEATURE_DIRECTORY=dummy-feature .specify/scripts/bash/check-prerequisites.sh --json` after creating a dummy feature and plan to verify that the core underlying bash scripts are functional and accessible.
7. Verified repository integrity by running `pnpm run build && ./jules-verify.sh`.

**What Gemini reviewed/contributed:**
- Gemini participated in the planning phase, reviewing the initial plan to ensure compliance with the repository's rules (Specificity, Verification, Groundedness, Pre-Commit, Completeness).
- Gemini guided the adjustment of steps to ensure accurate and granular execution, particularly around how verification steps must be explicit and grounded in actual tool output, and ensuring the `pnpm run build` requirement was met.

**How Spec Kit was invoked & Mechanism:**
- Spec Kit commands are currently invoked by the AI agent reading the relevant `.md` file in `.jules/cmds/` and acting upon its instructions.
- The new mechanism (Section 19 in `AGENTS.md`) formally establishes this reading-and-following approach as the official invocation method for Jules.

**What repository files were changed:**
- `AGENTS.md`: Appended Section 19.

**What verification was performed:**
- Verified the `AGENTS.md` update using `tail -n 30`.
- Ran a dummy workflow test to ensure `.specify/scripts/bash/check-prerequisites.sh` executes correctly.
- Ran `pnpm install --frozen-lockfile`, `pnpm run build` and `./jules-verify.sh` to ensure the build and test suites pass.
