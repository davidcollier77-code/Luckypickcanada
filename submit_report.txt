Libraries Consulted / Used:
- None specifically for this Ultra Review pass.

Files changed:
- `scripts/refresh-docs.js` (Fixed `getUpstreamSha` to handle slash-containing branches cleanly, drained response in redirects, sanitized `sealed_token`).
- `scripts/test-refresh-docs.js` (Updated redirect test to assert exact count of `requestedUrls.length`).
- `jules-verify.sh` (Added execution of `test-refresh-docs.js`).
- `.docs/manifest.json` (Added actual `githubShas` for new libraries using `git ls-remote`).
- `.docs/deep-dive/_posthog_posthog-js.md` (Fixed contributor repo and upstream links).
- `.docs/troubleshooting/_websites_neon.md` (Fixed duplicated title).
- `.docs/security/_upstash_ratelimit.md` (Made example explicitly server-side, removed exposed env hints).
- `.docs/polishing/_magicuidesign_magicui.md` (Updated Discord link to HTTPS).
- `.docs/security/_marsidev_react-turnstile.md` (Added alt text, `import type { FormEvent }`, replaced LICENSE link).
- `.docs/creation/_pmndrs_react-three-fiber.md` (Fixed React 18 versions).

Exact updater fixes implemented:
- Safely parse branch from GitHub raw URL even if the branch contains a slash, using a known branches allow-list (main, master, canary, develop, production).
- Properly call `res.resume()` to drain HTTP responses before redirecting.
- Add basic sanitization step `replace(/sealed_token=[^&"'\s]+/g, 'sealed_token=REDACTED')`.

Exact libraries added:
- N/A for this step, previously added.

Tests actually run:
- `pnpm run build` completed successfully.
- `./jules-verify.sh` executed `test-refresh-docs.js` which passed all 12 tests including the newly updated redirect loop request count test.

Limitations that remain:
- Context7 API is untouched.

External tools/MCPs invoked:
- None.
